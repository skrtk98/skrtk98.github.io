# 数学教科書原稿の作業規約

このリポジトリは、数学の教科書的資料を Markdown 原稿として管理し、Crossnote で HTML に変換して GitHub Pages で公開する。
原稿の正確性、用語と記号の一貫性、読者が論証を追える構成を優先する。

## リポジトリの構成

- `manuscripts/`：公開する書籍の Markdown 原稿。編集対象は原則としてこのディレクトリ以下とする。
- `manuscripts/contents/`：複数の書籍から参照する基礎事項や共通章。
- `site/`：Crossnote から出力した公開用 HTML。生成物であり、原稿の代わりに直接編集しない。
- `TopologyFormalization/`：位相空間論の原稿に対応する Lean 4 と mathlib の検証プロジェクト。
- `scripts/sync-site.sh`：Crossnote の出力を `site/` に配置する公開用スクリプト。

## 基本方針

- 既存の未コミット変更を作業前に確認し、今回の作業と無関係な変更を上書き、削除、コミットしない。
- 日本語の数学原稿を執筆、推敲、構成変更するときは、作業開始前に `japanese-academic-writing` と `japanese-tech-writing` のスキル指示を読み、両方を適用する。
- `japanese-academic-writing` は主張と根拠の対応、定義と証明の区別、断定の範囲、出典の扱いに適用する。
- `japanese-tech-writing` は一文一行、段落構成、見出し、用語導入、数式や補足の配置、冗長表現の抑制に適用する。
- スキルの指示と既存原稿の表記が衝突する場合は、数学的正確性とユーザーの明示的な指示を優先し、採用した判断を作業報告に記す。
- 原稿を変更した場合は、変更対象の章と、必要に応じて参照元および参照先を確認する。
- `site/` の HTML を手作業で修正せず、原稿を修正してから `scripts/sync-site.sh` で再生成する。
- 生成物、ロックファイル、設定ファイルは、変更が必要な作業でない限り編集しない。
- 書籍間で共通する定義や記号を変更するときは、利用箇所と公開対象の全書籍を確認する。

## 日本語の文体

- 常体（「である」調）を用い、敬体と混在させない。
- 一文ごとに改行し、段落の区切りは空行で示す。
- 一つの段落には、一つの主張、定義、導出、手続、または帰結だけを置く。
- 段落の冒頭で主題または結論を示し、後続文に条件、根拠、導出、含意を配置する。
- 事実、定義、仮定、推論、例、評価、未検証事項を区別して記述する。
- 「明らかである」「容易に分かる」「当然である」「自明である」など、証明や根拠を省略する表現を使わない。
- 根拠の範囲を超えて断定せず、「本稿の範囲では」「未検証である」「仮定する」などの限定を明記する。
- 読者への呼びかけ、会話的な表現、過度な強調、根拠のない評価語を避ける。
- 日本語の地の文と見出しでは em ダッシュ、horizontal bar、いわゆる二倍ダッシュを使わない。
- 日本語の並列では中黒を使わない。ただし固有名詞の一部は除く。
- 内容を特定できる見出しを付ける。「重要なこと」「まとめ」など情報量のない見出しは避ける。
- 太字は、初出の定義語または論理上必要な箇所に限る。

## 数学的な記述

- 定義、仮定、補題、定理、命題、系、例、反例、証明、注意を役割ごとに分ける。
- 定義や定理環境は、既存原稿の `Definition. **名称**`、`Theorem. **名称**` などの表記に合わせる。
- 定理環境の本文は、既存原稿と同じ定義リスト形式を使う。環境名と本文のインデントを崩さない。
- 新しい専門用語は初出時に定義するか、定義済みの節または出典を参照する。
- 記号、対象範囲、添字、上付き、写像の向きは導入時に定め、同一書籍内で一貫させる。
- 例は概念の理解を補うために用い、例だけを一般性の根拠にしない。反例は、どの仮定が必要かを示すために用いる。
- 証明では、使用する仮定、既知の結果、主要な推論を読者が追える順序で記述する。
- 証明を省略する場合は、単に「明らか」とせず、省略する理由または参照先を示す。
- 定義や定理の内容を変更した場合は、関連する例、証明、後続の命題との整合性を確認する。

## 数式と Markdown

- インライン数式は既存原稿の記法に合わせる。ブロック数式は ` ```math ` フェンスを用いる。
- 数式環境では、長い式を意味のまとまりごとに改行し、本文の表示幅を超えないようにする。
- `\(`、`\)`、`\[`、`\]` が原稿の変換後に残らないようにする。
- 定義リスト、箇条書き、引用、コードブロック、数式フェンスの前後には空行を置く。
- 独自 LaTeX マクロに依存する記述を新たに導入しない。既存マクロを使う場合も、公開先で解釈できることを確認する。
- HTML のインライン記法や既存原稿固有の `$``...``$` 記法を変更するときは、Crossnote の表示結果を確認する。
- 数式内の記号が HTML と衝突する可能性がある場合は、既存原稿の実績がある LaTeX 表記を使う。

## 図式と図表

- 図式や図表は、本文の主張と対応する位置に置き、図だけで必要な条件が失われないように説明する。
- TikZ または図式を変更した場合は、図の個数、矢印の向き、ラベル、対応関係を確認する。
- 図中の日本語はコンパイルや表示に失敗する可能性があるため、数式または英字ラベルを優先し、説明は本文に置く。
- 図式の変換では、未変換のプレースホルダー、古い `xymatrix` 記法、表示されない生の命令を残さない。

## 数学的検証と Lean

- 数学的主張は、形式化できる場合には Lean で検証することを優先する。
- Lean で検証済みの事実、文献に基づく事実、本文からの推論、作業仮説、未形式化の事項を混同しない。
- Lean で未検証の主張を確定的な定理として書かない。本文に記載する場合は、未検証または未形式化であることを明記する。
- 有限例だけを検証した場合、それを一般定理の検証結果として扱わない。
- 新しい理論的主張を追加した場合は、対応する Lean の宣言、docstring、ビルド結果との対応を確認する。
- `TopologyFormalization/` では、証明に `sorry`、`axiom`、`opaque` を用いない。
- Lean の検証は、次のコマンドを `TopologyFormalization/` で実行する。

  ```sh
  lake update
  lake exe cache get
  lake build
  ```

- Web や文献に基づく数学的主張を追加する場合は、参照元を原稿の近接した位置または参考文献に記録する。
- 出典は、実際に支持する主張の範囲を超えて使わない。

## 公開と検証

- 原稿の Markdown を変更したら、対象ファイルの見出し、定義リスト、数式フェンス、リンクを確認する。
- 公開用 HTML を更新する場合は、まず Crossnote で3冊の原稿をエクスポートし、既定の出力先に配置する。
- その後、リポジトリのルートで次を実行する。

  ```sh
  ./scripts/sync-site.sh
  ```

- `site/` の差分に、対象外の書籍や不要な大規模変更が含まれていないことを確認する。
- 数式が本文に露出していないこと、図式が欠落していないこと、長い式が画面幅で切れていないことを、生成 HTML のブラウザ表示で確認する。
- Lean に関係する変更では、`TopologyFormalization/` の `lake build` を実行する。
- 既存の警告と今回追加した警告を区別し、検証できなかった項目は最終報告に明記する。

## 変更の報告

- 最終報告では、変更したファイル、原稿上の変更内容、実行した検証コマンド、未検証の事項を具体的に示す。
- 公開用 HTML を更新した場合は、対応する Markdown 原稿と同じ変更として扱い、両者の対応を確認する。
- コミットを作成する場合は、意味のある変更単位で分け、無関係な変更を含めない。
